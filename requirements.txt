fastapi>=0.105.0
uvicorn[standard]>=0.23.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
requests>=2.31.0
python-multipart>=0.0.6
pytest>=8.0.0
httpx>=0.26.0

# Sprint 2: LLM & Memory
langchain>=0.1.0
langgraph>=0.0.1
# >=2.1 exposes the native google_search grounding tool for Gemini 2.x.
langchain-google-genai>=2.1.0
qdrant-client
langchain-community>=0.0.10
# IANA timezone database so zoneinfo resolves America/Bogota in slim containers
# (used by domain/datetime_context).
tzdata

# Optional provider libraries
faster-whisper>=0.8.0

#Grpc dependencies
grpcio==1.81.1
grpcio-tools==1.81.1
protobuf==6.33.6
setuptools==82.0.1
typing_extensions==4.15.0

# Authentication (HU-27 / ATOM-54)
motor>=3.4.0            # async MongoDB driver (user store)
argon2-cffi>=23.1.0     # Argon2id password hashing
pyjwt[crypto]>=2.8.0    # session access tokens (JWT)
google-auth>=2.28.0     # Google ID token verification (OIDC)
redis>=5.0.0            # refresh-token store (revocable, TTL)
mongomock-motor>=0.0.30 # test-only: in-memory async Mongo for unit tests
